export interface TvScreenState {
  opacity: number,
  hexColor: string
}

export interface TvRect {
  x: number,
  y: number,
  width: number,
  height: number
}

export const PDF_URL_KEY = "pdfUrl"

